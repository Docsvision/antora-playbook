server {
    listen 80;

    location = /.etc/nginx/rewrite.conf {
        deny all;
        return 404;
    }

    include /usr/share/docsvision/html/.etc/nginx/rewrite.conf;

    location / {
        root /usr/share/docsvision/html;
        index index.html;
    }

    error_page 404 /404.html;
}
